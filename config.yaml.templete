http_server:
  addr: "localhost:9999"
grafana_server:
  addr: "localhost:3000"
log:
  level: debug
  driver: file # 可选stdout和file。stdout：输出到终端控制台；file：输出到path下的指定文件。
  path: ./log/grafana.log
  max_file: 1
  max_size: 10485760
